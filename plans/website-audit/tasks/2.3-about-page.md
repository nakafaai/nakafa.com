# Task 2.3: Create About Page with Team Information

## 🎯 Goal

Create a comprehensive About page that establishes trust, showcases the team, and improves E-E-A-T signals.

## 📍 Context

**Current State**: No About page exists. This hurts:
- E-E-A-T score: 49/100 (should be >80)
- User trust and credibility
- Google's quality assessment
- Brand transparency

**Why This Matters**:
- About pages are crucial for trust signals
- Google checks for "About Us" pages for quality
- Required for YMYL (Your Money Your Life) content
- Helps users understand who is behind the content

## PRD

```json
{
  "category": "seo",
  "description": "Create About page with company info, team members, and trust signals",
  "steps": [
    "Create About page layout",
    "Add company mission and values section",
    "Create TeamSection component",
    "Add team member cards with photos and bios",
    "Add contact information",
    "Add credentials and expertise section",
    "Implement Organization structured data",
    "Link in footer navigation",
    "Test mobile responsiveness"
  ],
  "passes": false
}
```

## 🎬 Success Criteria

- [ ] About page accessible at `/about`
- [ ] Company mission and values displayed
- [ ] Team members shown with photos and bios
- [ ] Contact information visible
- [ ] Credentials and expertise highlighted
- [ ] Organization structured data implemented
- [ ] Page linked in footer
- [ ] Mobile-responsive design
- [ ] All tests pass

## Commands

```bash
# From root directory
pnpm lint
pnpm typecheck
pnpm test
```

## 📝 Implementation

### Subtask 2.3.1: Create About Page

**File to create**: `apps/www/app/[locale]/(study)/(main)/about/page.tsx`

```typescript
import { TeamSection } from "@/components/about/team-section";
import { OrganizationJsonLd } from "@repo/seo/json-ld/organization";
import { getTranslations } from "next-intl/server";

export async function generateMetadata({
  params,
}: {
  params: { locale: string };
}) {
  const t = await getTranslations({ locale: params.locale, namespace: "About" });
  return {
    title: t("title"),
    description: t("description"),
  };
}

export default function AboutPage({
  params: { locale },
}: {
  params: { locale: string };
}) {
  return (
    <>
      <OrganizationJsonLd
        name="Nakafa"
        url={`https://nakafa.com/${locale}`}
        logo="https://nakafa.com/logo.svg"
        description="Platform pembelajaran matematika dan sains"
        sameAs={[
          "https://github.com/nakafaai",
          "https://discord.gg/CPCSfKhvfQ",
          "https://www.youtube.com/@nakafaa",
        ]}
      />
      
      <div className="container mx-auto px-4 py-12 max-w-4xl">
        <section className="mb-16">
          <h1 className="text-4xl font-bold mb-6">About Nakafa</h1>
          <p className="text-lg text-muted-foreground mb-6">
            Nakafa adalah platform pembelajaran matematika dan sains yang
            bertujuan untuk membuat pembelajaran lebih mudah dipahami dan
            diakses oleh semua orang.
          </p>
          
          <div className="grid md:grid-cols-2 gap-8 mt-8">
            <div>
              <h2 className="text-2xl font-semibold mb-4">Our Mission</h2>
              <p className="text-muted-foreground">
                Menyediakan konten pembelajaran berkualitas tinggi yang mudah
                dipahami dan diakses oleh semua kalangan.
              </p>
            </div>
            
            <div>
              <h2 className="text-2xl font-semibold mb-4">Our Values</h2>
              <ul className="list-disc list-inside text-muted-foreground">
                <li>Kualitas konten terbaik</li>
                <li>Aksesibilitas untuk semua</li>
                <li>Inovasi dalam pembelajaran</li>
                <li>Komunitas yang supportive</li>
              </ul>
            </div>
          </div>
        </section>

        <TeamSection locale={locale} />

        <section className="mt-16">
          <h2 className="text-2xl font-semibold mb-4">Contact Us</h2>
          <p className="text-muted-foreground">
            Email: contact@nakafa.com
            <br />
            Discord:{" "}
            <a
              href="https://discord.gg/CPCSfKhvfQ"
              className="text-primary hover:underline"
            >
              Join our community
            </a>
          </p>
        </section>
      </div>
    </>
  );
}
```

### Subtask 2.3.2: Create TeamSection Component

**File to create**: `apps/www/components/about/team-section.tsx`

```typescript
import { Avatar, AvatarFallback, AvatarImage } from "@repo/design-system/components/ui/avatar";

interface TeamMember {
  name: string;
  role: string;
  bio: string;
  image?: string;
}

const team: TeamMember[] = [
  {
    name: "Nabil Akbarazzima Fatih",
    role: "Founder & Lead Developer",
    bio: "Mathematics enthusiast and software engineer passionate about making education accessible.",
    image: "/team/nabil.jpg",
  },
  // Add more team members as needed
];

interface TeamSectionProps {
  locale: string;
}

export function TeamSection({ locale }: TeamSectionProps) {
  return (
    <section>
      <h2 className="text-3xl font-bold mb-8">Our Team</h2>
      
      <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        {team.map((member) => (
          <div
            key={member.name}
            className="border rounded-lg p-6 text-center"
          >
            <Avatar className="h-24 w-24 mx-auto mb-4">
              <AvatarImage src={member.image} alt={member.name} />
              <AvatarFallback className="text-2xl">
                {member.name.charAt(0)}
              </AvatarFallback>
            </Avatar>
            
            <h3 className="font-semibold text-lg">{member.name}</h3>
            <p className="text-sm text-primary mb-2">{member.role}</p>
            <p className="text-sm text-muted-foreground">{member.bio}</p>
          </div>
        ))}
      </div>
    </section>
  );
}
```

### Subtask 2.3.3: Create OrganizationJsonLd Component

**File to create**: `packages/seo/src/json-ld/organization.tsx`

```typescript
import type { Organization, WithContext } from "schema-dts";

interface OrganizationJsonLdProps {
  name: string;
  url: string;
  logo: string;
  description?: string;
  sameAs?: string[];
}

export function OrganizationJsonLd({
  name,
  url,
  logo,
  description,
  sameAs,
}: OrganizationJsonLdProps) {
  const organization: WithContext<Organization> = {
    "@context": "https://schema.org",
    "@type": "Organization",
    name,
    url,
    logo: {
      "@type": "ImageObject",
      url: logo,
    },
    description,
    sameAs,
  };

  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(organization) }}
    />
  );
}
```

### Subtask 2.3.4: Add Link to Footer

**File to modify**: `apps/www/components/about/footer.tsx`

Add About link to footer navigation:

```typescript
<Link href="/about" className="hover:underline">About Us</Link>
```

---

## 🚀 Next Steps

After this task:
- Next: Task 3.1 - Preload Critical Resources (Performance)
- Dependencies: None

## 🔗 Related Tasks

- Task 2.1: Article Structured Data (Organization schema)
- Task 2.2: Author Information (team members)
- Task 5.3: Breadcrumb Navigation (page hierarchy)

## ⚠️ Important Notes

### Team Member Information

Collect for each team member:
- Full name
- Role/position
- Short bio (2-3 sentences)
- Professional photo (optional)
- Social links (optional)

### Organization Schema

Required fields:
- Name
- URL
- Logo

Optional but recommended:
- Description
- SameAs (social profiles)
- ContactPoint

### Design Considerations

- Clean, professional layout
- Easy to scan
- Mobile-first design
- Fast loading (optimize images)

---

## Progress Update Template

After completing this task, update `plans/website-audit/progress.txt`:

```txt
[YYYY-MM-DD HH:mm] Task 2.3 completed - About Page

Key decisions:
- Created comprehensive About page
- Added team section with member cards
- Implemented Organization structured data
- Linked in footer navigation

Files created:
- apps/www/app/[locale]/(study)/(main)/about/page.tsx
- apps/www/components/about/team-section.tsx
- packages/seo/src/json-ld/organization.tsx

Files modified:
- apps/www/components/about/footer.tsx
- packages/seo/src/index.ts

Verification:
- Page accessible at /about
- Organization schema validates
- Mobile layout tested
- Footer link works

Blockers/notes:
- None
```
