/** Read-only annotations shared by every public Nakafa MCP tool. */
export const NAKAFA_READ_ONLY_TOOL_ANNOTATIONS = {
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
  readOnlyHint: true,
} as const;
