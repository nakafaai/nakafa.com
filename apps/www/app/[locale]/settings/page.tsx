import { SettingsAccount } from "@/components/settings/account";

export default function Page() {
  return (
    <main className="relative flex h-[calc(100svh-4rem)] items-center justify-center lg:h-svh">
      <SettingsAccount />
    </main>
  );
}
