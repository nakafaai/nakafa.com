import { SchoolNotFound } from "@/components/school/not-found";

/** Render the shared Nakafa School 404 screen for the school route family. */
export default function NotFound() {
  return <SchoolNotFound />;
}
