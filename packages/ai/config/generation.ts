/** Chat generation lifecycle codes stored with persisted assistant messages. */
export const chatResponseFailureCode = "CHAT_RESPONSE_FAILED";

export const CHAT_GENERATION_FAILURE_CODES = [chatResponseFailureCode] as const;
