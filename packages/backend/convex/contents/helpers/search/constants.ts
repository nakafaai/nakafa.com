/** Default item count for paginated Nakafa content search results. */
export const CONTENT_SEARCH_DEFAULT_LIMIT = 20;

/** Hard cap for paginated Nakafa content search results. */
export const CONTENT_SEARCH_MAX_LIMIT = 50;

/** Hard cap for offset pagination within Convex full-text scan limits. */
export const CONTENT_SEARCH_MAX_OFFSET = 950;

/** Hard cap for query variants inside one bounded content search request. */
export const CONTENT_SEARCH_MAX_QUERIES = 4;
